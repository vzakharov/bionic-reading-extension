let settings = {

  // Hotkey to enable bionic reading
  hotkey: {
    modifier: 'alt',    // Only 'alt', 'ctrl' and 'meta' (referring to the ⌘ key on Macs) are supported
    key: 'b'
  },

  // Increment by which to increase the boldness of the first half of each word
  boldnessIncrement: 300,

  // What part of the word to bolden (0 to 1)
  boldnessCutoff: 0.5

}